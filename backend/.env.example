# Sao chép thành .env (backend/.env). KHÔNG commit file .env.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/moviecafe
PORT=3000
SESSION_SECRET=doi-chuoi-nay-thanh-chuoi-ngau-nhien-dai
# Origin của Vite dev server (frontend) để bật CORS + cookie khi chạy tách cổng
WEB_ORIGIN=http://localhost:5173
NODE_ENV=development
TZ=Asia/Ho_Chi_Minh
